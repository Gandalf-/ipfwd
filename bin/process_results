#!/usr/local/bin/bash

echo -n "$1 average: "
  grep 'sec' < "$1" | 
  cut -f 8 -d ' ' | 
  awk -F : '{ sum += $1 } END { printf "%f", sum / NR; }'
echo -n " "
grep -oh '[MGK]Bytes' < "$1" | head -n 1
